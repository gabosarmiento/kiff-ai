# Core Backend Requirements - Lightweight version without ML dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
uvloop==0.19.0
httptools==0.6.1
orjson==3.10.6
python-multipart==0.0.9
httpx==0.27.0
beautifulsoup4==4.12.3
sqlalchemy==2.0.31
psycopg2-binary>=2.9.0  # PostgreSQL driver for SQLAlchemy

# Database storage (still needed for fallback vector storage)
lancedb>=0.5.0
pyarrow>=15.0.0
numpy>=1.26.0,<2.0.0
tantivy  # Required for LanceDB full text search

# AWS and infrastructure
boto3>=1.34.0
e2b-code-interpreter>=1.5.2

# AGNO framework (still needed for agent operations in launcher)
agno
groq
chonkie  # Required for semantic chunking in agno
pypdf  # Required for PDF processing in agno

# HTTP client for ML service communication
aiohttp>=3.9.0

# Email delivery
resend>=0.7.0
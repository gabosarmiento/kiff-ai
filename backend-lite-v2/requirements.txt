fastapi==0.111.0
uvicorn[standard]==0.30.1
uvloop==0.19.0
httptools==0.6.1
orjson==3.10.6
python-multipart==0.0.9
httpx==0.27.0
beautifulsoup4==4.12.3
sqlalchemy==2.0.31
# LanceDB stack (optional at runtime; used if installed)
lancedb>=0.5.0
pyarrow>=15.0.0
numpy>=1.26.0,<2.0.0
boto3>=1.34.0
e2b-code-interpreter>=1.5.2

# AGNO + Groq LLM provider and local embedder
agno
groq
chonkie  # Required for semantic chunking in agno

# CPU-only PyTorch for sentence transformers (much smaller than CUDA)
torch==2.3.1  # CPU wheel from PyPI (no +cu suffix)
transformers>=4.41.0
sentence-transformers>=3.0.0

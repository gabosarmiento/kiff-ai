import React from 'react';
import { ChatOutput, ChatOutputMessage } from './ChatOutput';

export default {
  title: 'Kiff UI/ChatLayout',
  parameters: { docs: { disable: true } },
  tags: ['hidden'],
};

// Intentionally no named story exports; this entry is hidden. If needed later,
// reintroduce a story like `export const Demo = () => <ChatOutput ... />`.

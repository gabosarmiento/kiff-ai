# Core ML Service Dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
uvloop==0.19.0
httptools==0.6.1
orjson==3.10.6
python-multipart==0.0.9
httpx==0.27.0

# Vector storage and search
lancedb>=0.5.0
pyarrow>=15.0.0
numpy>=1.26.0,<2.0.0
tantivy

# AI/ML stack - the heavy dependencies
torch==2.2.2  # CPU wheel from PyPI (no +cu suffix)
transformers>=4.41.0
sentence-transformers>=3.0.0

# AGNO agent framework + dependencies
agno
groq
chonkie  # Required for semantic chunking in agno
pypdf  # Required for PDF processing in agno

# Web scraping and processing
beautifulsoup4==4.12.3
# Example AWS environment (copy to private/aws.env and edit)
# This file is NOT loaded automatically by GitHub/CI. Local only.

# AWS region
AWS_REGION=eu-west-3

# Choose a local profile from your ~/.aws/credentials
AWS_PROFILE=default

# Optional: if you prefer static credentials (not recommended), uncomment:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Optional: custom ECR repo name
ECR_REPO=kiff/backend-lite-v2

# Optional: App Runner service name
APP_RUNNER_SERVICE=kiff-backend-lite-v2

# Optional: allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=https://your-vercel-domain,http://localhost:3000

# Backend runtime envs
DEFAULT_TENANT_ID=4485db48-71b7-47b0-8128-c6dca5be352d
ALLOW_TENANT_FALLBACK=false
